
{
  "name": "taskventure",
  "version": "1.0.0",
  "description": "A fantasy-themed gamified task management app",
  "main": "index.html",
  "scripts": {
    "build": "node build.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npm run build && npx cap sync",
    "cap:build": "npm run build && npx cap build android --no-open",
    "android:dev": "npm run cap:sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "@capacitor/splash-screen": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  },
  "author": "Lukstar83"
}
